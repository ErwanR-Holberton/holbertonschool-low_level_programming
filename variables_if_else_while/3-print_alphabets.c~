#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char alpha;

	for (alpha = 'a'; alpha <= 'Z'; alpha++)
	{
		putchar(alpha);
		if (alpha == 'Z')
			alpha = 'A' - 1;
	}
	putchar('\n');
	return (0);
}
