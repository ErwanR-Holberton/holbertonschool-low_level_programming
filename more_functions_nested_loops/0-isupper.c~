#include "main.h"

/**
 * _isupper - Entry point
 *
 * @c : c from main is it a uppercase ?
 * Return: 1 if upper
 */
void _isupper(int c)
{
	if ((c >= 65) && (c <= 90))
		return (1);
	else
		return (0);
}
