#include "main.h"

/**
 * _strlen - Entry point
 *
 * @s : points to string start
 * Return : string lenght
 */
int _strlen(char *s)
{
	int i = 0;

	for (i = 0; s[i] != '\0'; i++)
	{

	}
	return (i);
}
