#include "main.h"

/**
 * print_alphabet - check the code
 *
 * Return: Always 0.
 */
void print_alphabet(void)
{
	char alpha;

	for (alpha = 0; alpha < 10; alpha++)
		print_alphabet();
	_putchar('\n');
}
